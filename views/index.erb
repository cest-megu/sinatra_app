<%# メモの一覧（トップページ） %>
<button class="new_button" type="submit" onclick="location.href='/memos/new'">新規投稿</button>
<div class="memo_list">
  <%= @memos.map {|memo| "<a href=memos/#{memo[:id]}>#{memo[:title]}</a>"}.join("<br>") %>
</div>
